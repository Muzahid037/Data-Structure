#include<bits/stdc++.h>
using namespace std;
int main()
{
    /* vector<int>vtint;
    // vector<string>vtsrting;
     //vector<st>vtstrct;
     vtint.push_back(10);
     vtint.push_back(20);
     vtint.push_back(30);
     vtint.pop_back();
     for(int i=0;i<vtint.size();i++){
     cout<<vtint[i]<<" ";}
     cout<<endl;
     vtint.pop_back();
     for(int i=0;i<vtint.size();i++){
     cout<<vtint[i]<<" ";}         */

      vector< vector<double> > vd;
     vd.push_back(5);
     vd.push_back(15);
      vd.push_back(25);
      for(int i=0;i<3;i++){
         for(int j=0;j<3;j++){
     cout<<vd[i][j]<<endl;}}

   /* vector<int>a[100];
    a[0].push_back(10);
    a[0].push_back(50);
    a[1].push_back(15);
    a[2].push_back(20);
    for(int i=0; i<a[0].size(); i++)
    {
        cout<<a[0][i]<<" ";
    }       */

    /* vector<int>a(50,100);
      a[0].push_back(11);
      a[1].push_back(15);
      a[2].push_back(20);
      for(int i=0;i<3;i++){
      cout<<a[0][0]<<endl;} */


}
